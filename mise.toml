# used to test tool install and venv creation
[tools]
python = "3.10.11"
shellcheck = "latest"

[env]
_.python.venv = { path = ".venv", create = true }

[tasks.shellcheck]
run = "shellcheck **/*.sh"
alias = "sc"
